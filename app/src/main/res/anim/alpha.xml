<?xml version="1.0" encoding="utf-8"?>
<alpha xmlns:android="http://schemas.android.com/apk/res/android"

    android:duration = "2000"
    android:fromAlpha= "1.0"
    android:toAlpha="0.0"
    android:fillAfter="true"

    >


</alpha>

<!--
    <view animation 종류>
    duration : 지속시간 (밀리초 단위 : 5000이면 5초초
    fromAlpha : 투명도 시작값 1이면 불투명
    toAlpha : 투명도 종료값 0이면 투명
    fillAfter : true로 두면 마지막 상태 그대로 유지
    fillEnabled : true로 두면 애니메이션 종료 후에도 동작가능 (안써도 default가 true)
-->